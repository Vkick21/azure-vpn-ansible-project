[defaults]
# Inventory i podstawowe ustawienia używane przy każdym uruchomieniu.
inventory = inventory.ini
host_key_checking = True
interpreter_python = auto_silent
retry_files_enabled = False
timeout = 20

[ssh_connection]
# Jedno połączenie SSH wystarcza do wykonania wielu zadań.
pipelining = True